# 损失函数

## L1 Loss 和 MSE Loss
```py
F.l1_loss(pred, target)
F.mse_loss(pred, target)
```

计算原理：
```py
L1_loss = mean(|y_pred - y_true|)
MSE_loss = mean((y_pred - y_true)²)
```

特性:
- L1 Loss 对异常值，不敏感，更加鲁棒但是收敛较慢，适合于数据中有较多异常值/噪声的场景
- MSE Loss 对异常值非常敏感，但是收敛速度较快，适合于数据相对干净，噪声较小，且需要惩罚大误差的场景


# 参数更新

`backward` 只是计算并累积梯度（把 `grad` 累加到 `param.grad`），不会修改 `model` 参数。真正修改参数的是 `optimizer.step()`。

- `loss_for_backward.backward()`：将当前 `loss` 的梯度累加到每个参数的 `param.grad` 上（不是累加到 `optimizer` 本身）。

- `optimizer.step()`：读取这些 `param.grad` 并用当前学习率更新模型参数（这是实际修改参数的操作）。

- `optimizer.zero_grad(set_to_none=True)`：清空或置为 `None`，下一次 `backward` 时才会重新累加新的梯度。`set_to_none=True` 比把梯度置 0 更省内存/更快（会把 `grad` 设为 `None` 而不是分配全 0 张量）。

因此，梯度累加 trick（模型在实际更新参数前会累积多个 mini-batch 的梯度，从而实现“有效更大的 batch size”而不增加显存峰值）可以如下实现：

```py
# 注意下面一定要 / accumulate，否则相当于每次更新累加了 accumulate 次 loss，学习率被调大了
accumulate = int(cfg_dict.trainer.accumulate_grad_batches)
# 计算当前组的实际累积大小（处理最后一组可能不足 accumulate 的情况）
total_batches = len(train_loader)
group_start = (batch_idx // accumulate) * accumulate
group_size = min(accumulate, max(1, total_batches - group_start))
loss_for_backward = loss_for_backward / group_size

# 正常 backward 累加梯度
if use_amp:
    scaler.scale(loss_for_backward).backward()
else:
    loss_for_backward.backward()

# 每 accumulate 步或者 train_loader 走完一遍后才进行更新
should_step = ((batch_idx + 1) % accumulate == 0) or (batch_idx + 1 == len(train_loader))
if should_step:
    if use_grad_scaler:
        scaler.unscale_(optimizer)
    if cfg_dict.trainer.gradient_clip_val is not None:
        torch.nn.utils.clip_grad_norm_(
            model.parameters(),
            cfg_dict.trainer.gradient_clip_val,
        )

    metrics["loss/grad_norm"] = _compute_grad_norm(model.parameters())
    metrics["lr"] = optimizer.param_groups[0]["lr"]

    if use_grad_scaler:
        scaler.step(optimizer)
        scaler.update()
    else:
        optimizer.step()
    scheduler.step()
    optimizer.zero_grad(set_to_none=True)
```
