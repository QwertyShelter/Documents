# 硬链接和软链接的区别和使用

```bash
ln -s [源文件或目录] [目标链接名称]

# 验证软链接是否有效
ls -l
```

如果想迁移文件应该创建软链接，路径兼容性更好，会通过软链接自动重定向到新位置。同时用户和程序无感知，如果将来需要再次迁移，只需要更新软链接的目标即可

硬链接问题：不能跨文件系统（如果新旧位置在不同磁盘分区就不行）；如果将来想真正删除原路径的访问会很麻烦；不够直观，管理起来不如软链接方便
